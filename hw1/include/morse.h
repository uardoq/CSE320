#ifndef MORSE_H
#define MORSE_H

#include "debug.h"

#endif

const char* translate_to_morse_code(char msg);

void create_morse_key();

char morse_encrypt();


char _morse_decrypt();
char morse_decrypt();

char translate_to_plain_text(const char *morse);
